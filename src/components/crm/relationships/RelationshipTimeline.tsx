import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import type { RelationshipInteraction } from '@/domain/relationships/contracts';
export function RelationshipTimeline({ items, title = 'Relationship timeline' }: { items: RelationshipInteraction[]; title?: string }) { return <Card><CardHeader><CardTitle>{title}</CardTitle><CardDescription>Relationship-only events; never clinical client activity.</CardDescription></CardHeader><CardContent>{items.length ? <ol className="space-y-3">{items.map(item => <li key={item.id} className="border-b pb-3 last:border-0"><p className="font-medium">{item.type.replaceAll('_', ' ')}</p><p className="text-sm text-muted-foreground">{item.summary}</p><p className="text-xs text-muted-foreground">{item.occurredAt}{item.actorId ? ` · ${item.actorId}` : ''}</p></li>)}</ol> : <p className="text-sm text-muted-foreground">No relationship activity is available.</p>}</CardContent></Card>; }
