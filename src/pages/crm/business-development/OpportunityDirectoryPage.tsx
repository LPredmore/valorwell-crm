import { Link } from 'react-router-dom';
import { Button } from '@/components/ui/button'; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import { RelationshipCapabilityState } from '@/components/crm/relationships/RelationshipCapabilityState'; import { useRelationshipCapability } from '@/hooks/relationships/useRelationshipCapabilities';
export default function OpportunityDirectoryPage() { const { capability, isLoading, isError, refetch } = useRelationshipCapability('opportunities'); return <div className="space-y-6"><div className="flex justify-between gap-3"><div><h1 className="text-3xl font-bold">BTY opportunities</h1><p className="mt-2 text-muted-foreground">Beyond The Yellow opportunities are relationship records, never clinical campaigns.</p></div><Button asChild variant="outline"><Link to="/crm/business-development/status">System status</Link></Button></div><Card><CardHeader><CardTitle>Opportunity filters</CardTitle><CardDescription>Search and filter by status, organization, contact, owner, veteran priority, cause area, review, risk, next action, and source.</CardDescription></CardHeader><CardContent className="grid gap-3 md:grid-cols-3"><div><Label htmlFor="opportunity-search">Search</Label><Input id="opportunity-search" placeholder="Organization or opportunity" /></div><div><Label htmlFor="opportunity-status">Status</Label><Input id="opportunity-status" placeholder="e.g. qualified" /></div><div><Label htmlFor="opportunity-cause">Cause area</Label><Input id="opportunity-cause" placeholder="Cause area" /></div></CardContent></Card><RelationshipCapabilityState state={capability} isLoading={isLoading} isError={isError} onRetry={() => { void refetch(); }} /><Card><CardHeader><CardTitle>Opportunity results</CardTitle><CardDescription>Results, qualification scoring, and outreach actions will load only from the typed opportunities adapter.</CardDescription></CardHeader><CardContent><p className="text-sm text-muted-foreground">Database support pending; no BTY opportunity is inferred from clinical or inbound-interest records.</p></CardContent></Card></div>; }
