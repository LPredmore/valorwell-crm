import { describe,expect,it } from 'vitest'; import { normalizeHandle,normalizePhone,normalizeState,normalizeYesNo,parseCsvRows } from '@/domain/relationships/import-normalization';
describe('import normalization',()=>{it('normalizes common import values',()=>{expect(normalizePhone('(555) 222-3333')).toBe('+15552223333');expect(normalizeState(' va ')).toBe('VA');expect(normalizeYesNo('Yes')).toBe(true);expect(normalizeHandle('@ValorWell')).toBe('valorwell');});it('parses quoted CSV safely',()=>expect(parseCsvRows('name,summary\nA,"hello, world"').rows[1]).toEqual(['A','hello, world']));});
