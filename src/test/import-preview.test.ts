import {describe,expect,it}from'vitest';import{previewRelationshipImport}from'@/domain/relationships/import-preview';describe('import preview',()=>it('classifies create duplicate ambiguous and invalid rows',()=>{const p=previewRelationshipImport('organization_name\nA\nB\n\n',{organization_name:'organization_name'},{3:[{entity:'organization',id:'x',score:80,signals:['name']]});expect(p.rows.map(r=>r.decision)).toEqual(['create','ambiguous','invalid']);}));
