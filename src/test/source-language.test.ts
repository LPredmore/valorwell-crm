import { describe, expect, it } from 'vitest'; import { sourceLanguageSentence } from '@/domain/relationships/source-language';
describe('source language', () => { it('never renders internal referral identity', () => expect(sourceLanguageSentence({ id:'r', createdAt:'',updatedAt:'',sourceCategory:'client',summary:'',evidenceUrls:[],verified:true,disclosure:'internal',namedReferrer:'Private' }).sentence).toBeUndefined()); it('renders a verified named referrer only when permitted', () => expect(sourceLanguageSentence({ id:'r',createdAt:'',updatedAt:'',sourceCategory:'staff',summary:'',evidenceUrls:[],verified:true,disclosure:'named_referrer',namedReferrer:'Pat' }).sentence).toContain('Pat')); });
