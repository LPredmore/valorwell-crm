project_id = "ahqauomkgflopxgnlndd"

[functions.crm-resend-email]
# Public Resend webhooks share this endpoint with JWT-authenticated CRM actions.
# The function performs signature verification or user/capability authorization internally.
verify_jwt = false

[functions.crm-resend-staff-broadcast]
# The function verifies the user JWT and CRM capability internally before claiming or sending work.
verify_jwt = false

[functions.ringcentral-sms]
verify_jwt = false

[functions.campaign-scheduler]
verify_jwt = false

[functions.relationship-resend-webhook]
verify_jwt = false

[functions.provider-applicant-communication-worker]
verify_jwt = false

[functions.campaign-trigger-worker]
verify_jwt = false

[functions.newsletter-send-worker]
verify_jwt = false

[functions.ai-operations-dispatcher]
# Scheduled AI Operations entrypoint; authorizes the service-role bearer in code.
verify_jwt = false

[functions.ai-operations-model-worker]
# Scheduled AI Operations model queue worker; authorizes the service-role bearer in code.
verify_jwt = false

[functions.social-media-manager]
# The function verifies the user JWT and CRM capability internally before reading or
# mutating social publishing data.
verify_jwt = false

[functions.video-youtube-publish-dispatcher]
# Scheduled worker that uploads approved publications to YouTube; authorizes the
# service-role bearer or X-Cron-Secret in code.
verify_jwt = false

[functions.youtube-oauth-link]
# Single-use setup link gated by its own random token, not a CRM session.
verify_jwt = false

[functions.youtube-oauth-callback]
# Receives Google's OAuth redirect; authorizes via state/PKCE, not a CRM session.
verify_jwt = false
